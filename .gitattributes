# Trained model weights are large binaries — store them via Git LFS.
*.pth filter=lfs diff=lfs merge=lfs -text
*.pt  filter=lfs diff=lfs merge=lfs -text

# Normalise line endings for text sources.
*.py   text eol=lf
*.js   text eol=lf
*.md   text eol=lf
